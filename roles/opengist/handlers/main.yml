---

- name: daemon-reload
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    force: true
  when:
    - ansible_facts.service_mgr | lower == "systemd"

- name: restart opengist
  become: true
  ansible.builtin.service:
    name: opengist
    state: restarted
  when:
    - not running_in_check_mode

- name: reload opengist
  become: true
  ansible.builtin.service:
    name: opengist
    state: reloaded
  when:
    - not running_in_check_mode

...
