#!/usr/bin/env bash
# hooks/dispatch
set -euo pipefail

dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"   # absoluter Hook-Pfad
cmd=${0##*/}                                         # „converge“, „verify“, …

exec "$dir/run_tox.sh" "$cmd" "$@"
